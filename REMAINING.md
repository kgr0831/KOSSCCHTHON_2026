# 두드리 남은 작업 및 버그

2026-09-20 기준 인계 메모. 실제 앱 저장소는 `C:/koss2026`, 브랜치는 `feat/MVP`이다. 사용자의 요청에 따라 이 목록을 정리한 시점에 기능 수정과 배포 작업은 멈췄다. 항목의 “미완료”는 사용자 제보를 기록했다는 뜻이며, 재현·원인 확인이나 수정 완료를 뜻하지 않는다.

## 완료된 작업과 아직 확인하지 않은 배포

- Supabase `dudri` 프로젝트(`jntscnmcynnlndkgceoo`)와 원본 로컬 SQLite의 **전체 사용자 데이터**를 초기화했다. 당시 별도 연결 재조회에서 앱 사용자·학교 인증·저장 문서·CLI 메타데이터·앱 세션과 Supabase Auth 사용자·세션이 모두 0건이었다. 학교 3곳, 도메인 3개, 태그 15개와 스키마는 유지했다. 이후 사용자가 재가입했다면 현재 건수는 달라질 수 있으므로 다시 초기화하지 않는다.
- PC 실행 시 8개 더미 계정을 다시 만들던 경로와 더미 로그인 API·화면을 제거했다. 커피챗 수신 설정은 저장 응답을 즉시 화면에 반영하고, 실패한 재조회가 값을 되돌리지 않게 수정했다. 수신을 끈 상대의 요청 버튼과 직접 링크의 작성 화면도 차단했다.
- 위 변경은 커밋 `6c76269`로 `origin/feat/MVP`에 푸시됐다. [CI 실행](https://github.com/kgr0831/KOSSCCHTHON_2026/actions/runs/35461320171)은 성공했다. 관련 로컬 백엔드 테스트 32개, 브라우저 테스트 12개, TypeScript 검사, Next.js 프로덕션 빌드, Ruff 검사를 통과했다. 초기화된 실제 DB에서 마이그레이션/기본 설정 준비도 성공했다.
- **남음:** Vercel Hobby 프런트가 `6c76269`를 운영 버전으로 반영했는지 확인한다. Render Free API(`srv-danavq3m8hqs73an6rh0`)는 자동 배포가 꺼져 있어 같은 커밋을 수동 배포하고 `/api/v1/health`와 공개 프로필의 커피챗 요청 가능 상태를 확인한다. 배포 전에 운영 버전에서 커피챗 UI 수정이 작동한다고 주장하지 않는다.
- 운영 확인은 새로운 실사용 계정을 임의로 만들거나 학교 인증 메일·커피챗 요청을 보내지 않는 범위에서 진행한다. 계정 재가입과 학교 이메일 재인증은 사용자가 실제로 이용할 때 필요하다. 초기화로 지워진 개인 CLI 연결 메타데이터는 필요할 때 PC에서 다시 기록한다.

## 이번 작업에서 구현됨 (배포 전 로컬 검증)

아래 항목은 이번 작업 트리에 구현·자동 검증까지 끝난 상태다. 실제 운영 배포 여부는 위의 배포 확인 항목과 별도로 확인한다.

1. **다가오는 커피챗 배치:** 예정된 커피챗을 화면 상단에 두고, 비어 있을 때는 컴팩트한 상태로 표시했다.
2. **커스텀 프로필 내비게이션:** 내부 공개 문서 뷰어에서도 데스크톱 사이드바와 모바일 하단 내비게이션이 유지되도록 수정했다.
3. **프로필 이미지 업로드 및 반영:** 파일 선택 업로드·DB 영속 저장·보호된 조회 경로를 추가해 내 화면과 공개 범위에 맞는 상대 화면에서 즉시 반영한다.
4. **실시간 UI·알림:** 인증된 일회용 WebSocket 티켓과 사용자별 이벤트 무효화 처리를 추가했다.
5. **내가 만든 프로젝트 삭제:** 생성자 전용 삭제 및 연결된 모집·요청·알림 정리를 구현했다.
6. **탐색의 ‘AI 조건 정리’ 버튼:** 요청·대기·성공·오류 상태와 편집 가능한 결과 반영을 연결했다.
7. **프로젝트 참여 상태·추천·신청 버튼:** 수락/정원 상태를 반영하고 참여 중이거나 마감된 프로젝트를 추천·신청 진입점에서 제외했다.
8. **요금제·AI 생성 권한:** 결제 연동 없이 FREE / PREMIUM 선택을 DB에 저장하고, 포트폴리오·CV·자기 PR 프로필·자기소개서 AI 생성은 PREMIUM에서만 실행되도록 서버와 화면을 함께 제한했다.
9. **포인트·월간 기회:** 완료된 커피챗 참석자 포인트 누적, 요청·모집 게시·참여 신청의 공통 월간 기회(FREE 5회 / PREMIUM 20회), 한국 시간 기준 월간 초기화, 멘토 수신자 1회 보너스를 DB에 저장한다.
10. **CLI 생성 메모리·참고 파일:** 각 포트폴리오·자기 PR 프로필·CV·자기소개서 생성에만 연결되는 비공개 참고 파일을 추가했다. PDF/TXT/DOCX/HTML은 추출 텍스트로, PNG/JPG는 연결된 로컬 Codex CLI의 이미지 입력으로 전달한다. 파일은 생성 버전에만 연결되고 공개 문서·스냅샷에는 원문을 남기지 않는다. 구형 `.doc`는 안전한 자동 추출 대신 `.docx`로 변환 안내를 제공한다.

## 재개할 때 확인할 것

- 실제 저장소에서 `frontend/next-env.d.ts`는 마지막 Next.js 빌드가 만든 변경으로 표시됐다. 별도의 `landing/` 폴더도 추적되지 않은 상태다. 둘 다 이번 커밋에 넣지 않았으며 다른 작업자의 변경 여부를 먼저 확인한다.
- `README.md`와 `IMPLEMENTATION.md`의 일부 과거 검증 설명에는 초기화 전 더미 계정·저장 문서 사례가 남아 있다. 현재 운영 데이터로 오해하지 않도록 문서 정리가 남았다.
- 원래 Architecture 전체 범위의 별도 미구현 항목은 [IMPLEMENTATION.md](IMPLEMENTATION.md)의 ‘전체 Architecture 기준으로 별도 남은 범위’를 참고한다. 이 문서의 1~10번은 이번 대화에서 새로 접수되어 로컬 구현한 버그·기능이다.
